import { ServerSortPipe } from './server-sort.pipe';

describe('ServerSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ServerSortPipe();
    expect(pipe).toBeTruthy();
  });
});
